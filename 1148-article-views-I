SELECT distinct(author_id) AS id
FROM view
WHERE author_id = viewer_id
ORDER BY author_id


OTHER SOLUTION
SELECT author_id as id
FROM view
WHERE author_id = viewer_id
GROUP BY author_id
ORDER BY author_id
